<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.name %> - LUXORA</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;500;600;700&family=Outfit:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg: #faf9f7;
            --fg: #1a1a1a;
            --muted: #6b6b6b;
            --accent: #c9a962;
            --card: #ffffff;
            --border: #e8e6e3;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: 'Outfit', sans-serif;
            background: var(--bg);
            color: var(--fg);
            line-height: 1.6;
        }

        /* Header */
        header {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            background: rgba(250, 249, 247, 0.95);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid var(--border);
        }

        .header-inner {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 40px;
            height: 80px;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .logo {
            font-family: 'Cormorant Garamond', serif;
            font-size: 2rem;
            font-weight: 600;
            letter-spacing: 0.3em;
            color: var(--fg);
            text-decoration: none;
        }

        .nav-links {
            display: flex;
            gap: 40px;
            list-style: none;
        }

        .nav-links a {
            font-size: 0.85rem;
            font-weight: 500;
            letter-spacing: 0.1em;
            text-transform: uppercase;
            color: var(--muted);
            text-decoration: none;
            transition: color 0.3s;
        }

        .nav-links a:hover { color: var(--fg); }

        .header-actions {
            display: flex;
            align-items: center;
            gap: 24px;
        }

        .icon-btn {
            background: none;
            border: none;
            color: var(--muted);
            font-size: 1.2rem;
            cursor: pointer;
            position: relative;
        }

        .icon-btn:hover { color: var(--fg); }

        .cart-count {
            position: absolute;
            top: -8px;
            right: -8px;
            background: var(--accent);
            color: white;
            font-size: 0.7rem;
            width: 18px;
            height: 18px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
        }

        /* Product Detail */
        .product-detail {
            max-width: 1400px;
            margin: 0 auto;
            padding: 140px 40px 80px;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 80px;
        }

        .product-gallery {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .main-image {
            aspect-ratio: 1;
            background: linear-gradient(135deg, #f0ede8 0%, #e5e1da 100%);
            border-radius: 12px;
            position: relative;
            overflow: hidden;
        }

        .main-image::before {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(45deg, rgba(201, 169, 98, 0.1) 0%, transparent 100%);
        }

        .thumbnail-row {
            display: flex;
            gap: 12px;
        }

        .thumbnail {
            width: 80px;
            height: 80px;
            background: var(--card);
            border: 2px solid var(--border);
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s;
        }

        .thumbnail:hover,
        .thumbnail.active {
            border-color: var(--accent);
        }

        .product-info {
            padding-top: 40px;
        }

        .breadcrumb {
            font-size: 0.85rem;
            color: var(--muted);
            margin-bottom: 20px;
        }

        .breadcrumb a {
            color: var(--muted);
            text-decoration: none;
        }

        .breadcrumb a:hover {
            color: var(--accent);
        }

        .product-category {
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 0.15em;
            color: var(--accent);
            margin-bottom: 12px;
        }

        .product-title {
            font-family: 'Cormorant Garamond', serif;
            font-size: 3rem;
            font-weight: 400;
            margin-bottom: 20px;
            line-height: 1.2;
        }

        .product-price {
            font-size: 1.8rem;
            font-weight: 600;
            margin-bottom: 24px;
        }

        .product-price .original {
            text-decoration: line-through;
            color: var(--muted);
            font-size: 1.2rem;
            margin-left: 12px;
        }

        .product-desc {
            color: var(--muted);
            margin-bottom: 40px;
            line-height: 1.8;
        }

        .product-options {
            margin-bottom: 40px;
        }

        .option-label {
            font-size: 0.9rem;
            font-weight: 500;
            margin-bottom: 12px;
        }

        .size-options {
            display: flex;
            gap: 12px;
        }

        .size-btn {
            width: 50px;
            height: 50px;
            border: 1px solid var(--border);
            background: var(--card);
            border-radius: 8px;
            font-family: inherit;
            font-size: 0.9rem;
            cursor: pointer;
            transition: all 0.3s;
        }

        .size-btn:hover,
        .size-btn.selected {
            border-color: var(--fg);
            background: var(--fg);
            color: white;
        }

        .quantity-selector {
            display: flex;
            align-items: center;
            gap: 20px;
            margin-bottom: 32px;
        }

        .qty-btn {
            width: 44px;
            height: 44px;
            border: 1px solid var(--border);
            background: var(--card);
            border-radius: 8px;
            font-size: 1.2rem;
            cursor: pointer;
            transition: all 0.3s;
        }

        .qty-btn:hover {
            border-color: var(--fg);
        }

        .qty-value {
            font-size: 1.2rem;
            font-weight: 500;
            min-width: 40px;
            text-align: center;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            padding: 18px 40px;
            font-family: inherit;
            font-size: 0.9rem;
            font-weight: 500;
            letter-spacing: 0.1em;
            text-transform: uppercase;
            text-decoration: none;
            border: none;
            cursor: pointer;
            transition: all 0.4s ease;
            border-radius: 4px;
        }

        .btn-primary {
            background: var(--fg);
            color: white;
            flex: 1;
        }

        .btn-primary:hover {
            background: var(--accent);
            transform: translateY(-2px);
        }

        .btn-outline {
            background: transparent;
            color: var(--fg);
            border: 1px solid var(--border);
            padding: 18px 24px;
        }

        .btn-outline:hover {
            border-color: var(--fg);
        }

        .action-row {
            display: flex;
            gap: 16px;
        }

        .product-meta {
            margin-top: 40px;
            padding-top: 40px;
            border-top: 1px solid var(--border);
        }

        .meta-item {
            display: flex;
            padding: 16px 0;
            border-bottom: 1px solid var(--border);
        }

        .meta-label {
            width: 120px;
            font-weight: 500;
            color: var(--muted);
        }

        .meta-value {
            flex: 1;
        }

        /* Reviews Section */
        .reviews-section {
            max-width: 1400px;
            margin: 0 auto;
            padding: 80px 40px;
            border-top: 1px solid var(--border);
        }

        .section-title {
            font-family: 'Cormorant Garamond', serif;
            font-size: 2rem;
            margin-bottom: 40px;
        }

        .review-form {
            background: var(--card);
            padding: 32px;
            border-radius: 12px;
            margin-bottom: 40px;
            border: 1px solid var(--border);
        }

        .review-form h3 {
            margin-bottom: 20px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 500;
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 12px 16px;
            border: 1px solid var(--border);
            border-radius: 8px;
            font-family: inherit;
            font-size: 0.95rem;
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: var(--accent);
        }

        .form-group textarea {
            min-height: 120px;
            resize: vertical;
        }

        /* Reviews List - VULN: Stored XSS here */
        .reviews-list {
            display: flex;
            flex-direction: column;
            gap: 24px;
        }

        .review-item {
            background: var(--card);
            padding: 24px;
            border-radius: 12px;
            border: 1px solid var(--border);
        }

        .review-header {
            display: flex;
            justify-content: space-between;
            margin-bottom: 12px;
        }

        .review-author {
            font-weight: 500;
        }

        .review-date {
            color: var(--muted);
            font-size: 0.9rem;
        }

        .review-content {
            color: var(--muted);
            line-height: 1.7;
        }

        /* Footer */
        footer {
            background: var(--fg);
            color: white;
            padding: 60px 40px;
            text-align: center;
        }

        .footer-logo {
            font-family: 'Cormorant Garamond', serif;
            font-size: 1.5rem;
            letter-spacing: 0.3em;
            margin-bottom: 20px;
        }

        .footer-text {
            color: rgba(255,255,255,0.5);
            font-size: 0.9rem;
        }

        @media (max-width: 900px) {
            .product-detail {
                grid-template-columns: 1fr;
                gap: 40px;
                padding: 120px 20px 60px;
            }
        }
    </style>
</head>
<body>
    <header>
        <div class="header-inner">
            <a href="/" class="logo">LUXORA</a>

            <nav>
                <ul class="nav-links">
                    <li><a href="/">New Arrivals</a></li>
                    <li><a href="/products">Women</a></li>
                    <li><a href="/products">Men</a></li>
                    <li><a href="/products">Accessories</a></li>
                </ul>
            </nav>

            <div class="header-actions">
                <button class="icon-btn" onclick="location.href='/search'">🔍</button>
                <button class="icon-btn">👤</button>
                <button class="icon-btn" onclick="location.href='/cart'">
                    🛒
                    <span class="cart-count">0</span>
                </button>
            </div>
        </div>
    </header>

    <main class="product-detail">
        <div class="product-gallery">
            <div class="main-image"></div>
            <div class="thumbnail-row">
                <div class="thumbnail active"></div>
                <div class="thumbnail"></div>
                <div class="thumbnail"></div>
                <div class="thumbnail"></div>
            </div>
        </div>

        <div class="product-info">
            <div class="breadcrumb">
                <a href="/">Home</a> / <a href="/products">Products</a> / <span><%= product.name %></span>
            </div>

            <div class="product-category"><%= product.category %></div>
            <h1 class="product-title"><%= product.name %></h1>

            <div class="product-price">
                $<%= product.price.toFixed(2) %>
                <% if (product.originalPrice) { %>
                <span class="original">$<%= product.originalPrice.toFixed(2) %></span>
                <% } %>
            </div>

            <p class="product-desc">
                Crafted with exceptional attention to detail, this piece embodies the perfect balance of timeless elegance and modern sophistication. Made from premium materials sourced from the finest artisans around the world.
            </p>

            <div class="product-options">
                <div class="option-label">Select Size</div>
                <div class="size-options">
                    <button class="size-btn">XS</button>
                    <button class="size-btn selected">S</button>
                    <button class="size-btn">M</button>
                    <button class="size-btn">L</button>
                    <button class="size-btn">XL</button>
                </div>
            </div>

            <div class="quantity-selector">
                <span class="option-label">Quantity</span>
                <button class="qty-btn" onclick="updateQty(-1)">-</button>
                <span class="qty-value" id="qty">1</span>
                <button class="qty-btn" onclick="updateQty(1)">+</button>
            </div>

            <div class="action-row">
                <button class="btn btn-primary" onclick="addToCart()">
                    Add to Cart
                </button>
                <button class="btn btn-outline">♡</button>
            </div>

            <div class="product-meta">
                <div class="meta-item">
                    <span class="meta-label">SKU</span>
                    <span class="meta-value"><%= product.id %></span>
                </div>
                <div class="meta-item">
                    <span class="meta-label">Category</span>
                    <span class="meta-value"><%= product.category %></span>
                </div>
                <div class="meta-item">
                    <span class="meta-label">Shipping</span>
                    <span class="meta-value">Free shipping on orders over $100</span>
                </div>
            </div>
        </div>
    </main>

    <!-- Reviews Section - VULN: Stored XSS in reviews -->
    <section class="reviews-section">
        <h2 class="section-title">Customer Reviews</h2>

        <!-- Review Form -->
        <div class="review-form">
            <h3>Write a Review</h3>
            <form action="/products/<%= product.id %>/reviews" method="POST">
                <div class="form-group">
                    <label for="author">Name</label>
                    <input type="text" id="author" name="author" required>
                </div>
                <div class="form-group">
                    <label for="content">Review</label>
                    <textarea id="content" name="content" required placeholder="Share your thoughts about this product..."></textarea>
                </div>
                <button type="submit" class="btn btn-primary" style="width: auto;">Submit Review</button>
            </form>
        </div>

        <!-- Reviews List - VULN: No sanitization, renders raw HTML -->
        <div class="reviews-list">
            <% if (reviews && reviews.length > 0) { %>
                <% reviews.forEach(review => { %>
                <div class="review-item">
                    <div class="review-header">
                        <span class="review-author"><%= review.author %></span>
                        <span class="review-date"><%= review.created_at %></span>
                    </div>
                    <div class="review-content">
                        <%- review.content %>
                    </div>
                </div>
                <% }); %>
            <% } else { %>
                <p style="color: var(--muted);">No reviews yet. Be the first to review this product!</p>
            <% } %>
        </div>
    </section>

    <footer>
        <div class="footer-logo">LUXORA</div>
        <p class="footer-text">© 2024 LUXORA. All rights reserved.</p>
    </footer>

    <script>
        let qty = 1;

        function updateQty(delta) {
            qty = Math.max(1, qty + delta);
            document.getElementById('qty').textContent = qty;
        }

        function addToCart() {
            alert('Added to cart!');
        }

        // Size selection
        document.querySelectorAll('.size-btn').forEach(btn => {
            btn.addEventListener('click', () => {
                document.querySelectorAll('.size-btn').forEach(b => b.classList.remove('selected'));
                btn.classList.add('selected');
            });
        });
    </script>
</body>
</html>
